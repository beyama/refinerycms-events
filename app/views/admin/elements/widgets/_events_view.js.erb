widgets.EventsView = ElementsEditor.ElementBaseView.extend({
  elementTitle: 'Events',

  className: 'element eventsView clearfix',

  initialize: function() {
    ElementsEditor.ElementBaseView.prototype.initialize.apply(this, arguments);
  },

  render: function() {
    ElementsEditor.ElementBaseView.prototype.render.apply(this, arguments);

    // this.richTextView = this.getWidgetForProperty('text', {
    //   togglable: false,
    //   headerless: true
    // });

    // this.bind('view:DOMNodeInserted', this.fixEditorSize, this);

    // this.$('.element-body:first').append(this.richTextView.render().el);

    return this;
  }

});

ElementsEditor.defaultWidgets['RichText'] = 'RichTextView';

/* vim: set filetype=javascript: */

